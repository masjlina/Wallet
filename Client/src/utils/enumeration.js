const urlPaths = {
    home: "#/",
    login: "#/login",
    registration: "#/registration",
    transactions: "#/transactions",
    wallet: "#/wallet",
    settings: "#/settings",
    logout: "#/logout"
}

export default urlPaths;